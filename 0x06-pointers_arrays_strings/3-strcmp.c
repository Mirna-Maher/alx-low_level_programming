#include "main.h"

/**
 * _strcmp -  a function that compares two strings
 * @s1: first string
 * @s2: second string
 * Return: 0 if they are same,
 * negative if s1< s2,
 * positive if s1> s2
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
