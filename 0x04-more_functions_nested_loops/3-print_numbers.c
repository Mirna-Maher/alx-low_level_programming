#include "main.h"

/**
 * print_numbers - function that will print numbers
 * i is the number
 * Return: void
 */
void print_numbers(void)
{
	char i;

	for (i >= 0; i <= 9; i++)
		putchar(i);
	putchar('\n');
}
